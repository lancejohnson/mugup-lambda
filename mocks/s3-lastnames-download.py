# // in mocks/process-lastnames-event.json can mock events with real JSON to deploy functions locally
# // with something like `npm run sls -- invoke --function process_lastname --path mocks/list-event.json`
# // (it must be serialized JSON, though, use something like this to convert it: )

# here we should mock the s3 bucket download of the lastname csv
# http://joshuaballoch.github.io/testing-lambda-functions/

